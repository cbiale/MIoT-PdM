/*
 * port.h
 *
 *  Created on: Dec 3, 2023
 *      Author: cbiale
 */

#ifndef API_INC_PORT_H_
#define API_INC_PORT_H_

#include "stm32f4xx_hal.h" // hal que voy a usar en el proyecto, permite portabilidad

// Funciones que abstraen la HAL


/**
 * @brief Inicializa el módulo I2C.
 *
 * Esta función realiza las operaciones necesarias para iniciar el módulo I2C.
 */
void I2C_iniciar();


/**
 * @brief Transmite un dato a través del bus I2C.
 *
 * Esta función envía el dato proporcionado a través del bus I2C. El parámetro
 * 'tamano' indica la cantidad de bytes a transmitir, y 'tiempoEspera' representa
 * el tiempo que la función debería esperar después de la transmisión para permitir
 * que el bus I2C se estabilice o realizar otras tareas necesarias.
 *
 * @param[in] dato El dato que se va a transmitir a través del bus I2C.
 * @param[in] tamano La cantidad de bytes a transmitir.
 * @param[in] tiempoEspera El tiempo de espera después de la transmisión.
 */
void I2C_transmitir (uint8_t dato, uint8_t tamano, uint8_t tiempoEspera);

/**
 * @brief Espera durante un período de tiempo especificado.
 *
 * Esta función realiza una espera durante el tiempo especificado.
 * La unidad de tiempo utilizada está definida por la implementación y debe ser ajustada
 * según los requisitos de tu aplicación.
 *
 * @param[in] tiempo El tiempo de espera en unidades definidas por tu aplicación.
 */
void esperar (uint32_t tiempo);
void manejar_errores(void);

#endif /* API_INC_PORT_H_ */
